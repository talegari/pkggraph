# pkggraph 0.3.1

* [maintenance release] Fixed clashing imports from `rlang`.

# pkggraph 0.3.0

* Complete rewrite of the package with only three functions: `init`, `get_dependencies`, `get_neighborhood`.
* Added a `NEWS.md` file to track changes to the package.
